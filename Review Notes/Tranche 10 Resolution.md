# Tranche 10 Resolution

Scope: post-2021 chemistry/materials feedback covering Bloch-orbital materials qubitization, condensed-phase Trotterization, force gradients, stopping power, quantum-advantage assessment, CYP P450, initial-state preparation, SOS/spectrum amplification, QFMM, BLISS-THC/AV compilation, RC-DF, and associated trick cards.

## Paper Notes

- `Fault-Tolerant Quantum Simulation of Materials Using Bloch Orbitals`: accepted the comparison-hygiene feedback. The scaling table now labels total phase-estimation Toffoli proxies rather than walk-step Toffolis, the sparse-LCU factor-of-two statement is tied to four-fold Bloch-integral symmetry, DF dominance is scoped to the compiled Toffoli/runtime table, and the first-quantized plane-wave comparison now defines the differing `N`, `N_k`, and grid/orbital conventions.
- `Improved Fault-Tolerant Quantum Simulation of Condensed-Phase Correlated Electrons via Trotterization`: accepted the finite-resource caveats. The note now shows the repeated second-order formula with `r` slices, states the `Wt^3 <= sqrt(2)` condition with the non-perturbative error claim, and separates extensive-error finite-size wins from intensive-error/asymptotic comparisons.
- `Efficient Quantum Computation of Molecular Forces and Other Energy Gradients`: accepted the force-block-encoding caveats. The THC `sqrt(2)` and `<=2x` statements are now construction-specific, the gradient algorithm comparison is scoped to the paper's model, finite-difference errors are defined more carefully, and a derivative-oracle warning now covers new PREPARE data, SELECT branches, Pulay terms, and response tensors.
- `Quantum Computation of Stopping Power for Inertial Fusion Target Design`: accepted the scope corrections. The "first constant-factor" claim is narrowed to stopping-power/real-time dynamics, projectile overhead is no longer described as absent, the empirical product-formula prefactor is marked as extrapolated from small simulations, and the output is linked to the downstream classical stopping-power workflow.
- `Is There Evidence for Exponential Quantum Advantage in Quantum Chemistry`: accepted the overstatement feedback. The paper is now described as skeptical evidence rather than a no-go theorem, `S` is labeled as amplitude overlap with standard QPE using `1/S^2`, the ansatz and ASP arguments are marked heuristic/empirical, and later MPS state-preparation work is added as updated context rather than a refutation.
- `Reliably Assessing the Electronic Structure of Cytochrome P450`: accepted the major initial-state correction. The note now distinguishes largest computational-basis configuration overlap from a simple Hartree-Fock claim, scopes runtime advantage to the paper's DMRG and surface-code/factory assumptions, and narrows CYP drug-metabolism relevance to motivation for the model.
- `Rapid Initial State Preparation for the Quantum Simulation of Strongly Correlated Molecules`: accepted the overlap and subroutine-scope feedback. The note now states the 2025 PRX Quantum publication, labels `p` as squared overlap, scopes the `7x` improvement to MPS/unitary synthesis rather than total QPE, and marks the `p \lesssim 0.003` crossover as parameter-dependent.
- `Postponing the Orthogonality Catastrophe`: accepted the historical-scope feedback. The note now says Tubman et al. weakened the simple orthogonality-catastrophe objection in studied systems rather than settling FeMoCo generally, separates compressed-register preparation from the isometry cost, and moves the lack of FT/T-count analysis into the main construction discussion.
- `Fast Quantum Simulation of Electronic Structure by Spectrum Amplification`: accepted the SOS/SA correctness feedback. The note now distinguishes abstract PSD factorization from efficient chemistry-structured SOS, defines `E_gap` as energy above the SOS lower bound, describes PE on the rectangular amplified walk rather than directly on `H_sqrt`, labels scaling fits as empirical, and corrects the Reiher comparison ratio to about `5e4` unless a different metric is specified.
- `Quantum Simulation of Chemistry via Quantum Fast Multipole Method`: accepted the version-sensitive correction. The note now follows the revised `N < eta^7` crossover, identifies the representation as real-space first quantization rather than plane-wave orbital LCU, fixes the sorting-cost expression, and separates evenly distributed near-field cost from adaptive shifted-order potential/sorting costs.
- `Faster Quantum Chemistry Simulations via Improved Tensor Factorization and Active Volume Compilation`: accepted the BLISS/AV separation feedback. BLISS shifts are now described as sector-constant symmetry shifts, the `100x` speedup is explicitly a runtime/device-compilation claim, and the reusable ideas no longer link BLISS-THC to DFTHC or QROAM symmetry adaptation.
- `Accelerating Quantum Computations of Chemistry Through Regularized Compressed Double Factorization`: accepted the attribution correction. The note no longer presents `lambda ~700`, `2.6e9` Toffolis, or `1994` logical qubits as Oumarou RC-DF results; those are separated as later SCDF/symmetry-shift context. RC-DF is now summarized as regularized CDF with a CCSD(T)-motivated heuristic penalty, distinct from SCDF, BLISS-THC, DFTHC, and SOS spectrum amplification.

## Trick Cards

- `k-Point THC Factorization` and `Controlled Momentum-Register Swaps`: scoped benchmark rank choices and no-speedup claims to the Bloch-orbital second-quantized construction, clarified that controlled-SWAP Toffoli counts are decomposition-level with Cliffords free, and replaced representation-independent lower-bound wording with explicit-routing cost intuition.
- `Force Operator Block Encoding via Hamiltonian Differentiation` and `FFFT Diagonalisation of Force Operators in Plane-Wave Basis`: added Hermiticity/dagger assumptions for the square identity, expanded derivative-data caveats, narrowed common-commuting-basis statements to plane-wave potential-force components, and clarified that equal asymptotic `lambda` scaling is not exact equality.
- `EQA Assessment Framework`, `ASCI Overlap Estimation`, `Partial-Column Unitary Synthesis`, and `MPS Overlap Extrapolation`: separated determinant, CSF, selected-CI, MPS amplitude, and squared-overlap notions; turned overlap/energy links into diagnostics rather than theorems; scoped `7x` to the synthesis subroutine; and marked MPS overlap extrapolation as empirical rather than a rigorous QPE success bound.
- `Rectangular Block-Encoding for Spectrum Amplification`, `SOS Decomposition via Semidefinite Programming for Chemistry`, `DFTHC Factorization`, and `SOS Spectral Amplification`: corrected the rectangular-map algebra (`A^\dagger A` encodes the shifted Hamiltonian), removed "phase estimation directly on `H_SA`" language, replaced "SOS for free" with an SDP/certification caveat, labeled DFTHC `N^2.09` as near-quadratic empirical scaling, and fixed broken LCU wikilink-in-subscript notation.

No supervisor suggestion in this tranche required rejecting the underlying feedback. A few items were implemented as scope clarifications rather than factual reversals, especially where the original note was correct only under a specific representation, hardware model, or resource metric.
